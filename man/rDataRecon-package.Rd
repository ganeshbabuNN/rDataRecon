% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rDataRecon-package.R
\docType{package}
\name{rDataRecon-package}
\alias{rDataRecon}
\alias{rDataRecon-package}
\title{rDataRecon: Dataset Comparison and Reconciliation Utility}
\description{
The \pkg{rDataRecon} package provides a comprehensive data frame
comparison and reconciliation tool. The main function \code{\link[=recon]{recon()}} compares a
BASE data frame against a COMPARE data frame and produces a structured
four-section report covering variable structure, type mismatches,
row-level value differences, and a summary count table.
}
\section{Main function}{

\itemize{
\item \code{\link[=recon]{recon()}}  run a full comparison and print/return the report.
}
}

\section{Helper functions}{

\itemize{
\item \code{\link[=get_diffs]{get_diffs()}}  extract the differences \code{data.frame} from a result.
\item \code{\link[=summary_df]{summary_df()}}  convert the summary list to a one-row \code{data.frame}.
\item \code{\link[=compare_stats]{compare_stats()}}  side-by-side numeric summary statistics.
\item \code{\link[=is_structure_equal]{is_structure_equal()}}  quick structural equality check.
}
}

\section{Key features}{

\itemize{
\item \strong{Zero hard dependencies}  base R only; no additional packages required.
\item \strong{Key-based or positional matching}  match rows by one or more ID
columns (\code{id} argument), or fall back to row position when \code{id = NULL}.
\item \strong{Cross-variable name mapping}  compare columns with different names
across datasets using \code{base_var} / \code{compare_var}.
\item \strong{Absolute and relative numeric tolerances}  \code{criterion} controls the
acceptable difference; set \code{relative = TRUE} for proportional comparison.
\item \strong{Plain-text report export}  \code{out = "file.txt"} writes the full report.
\item \strong{Programmatic result}  returns a structured \code{rDataRecon_result} list
for use in automated QC pipelines.
}
}

\section{Option reference}{
\tabular{ll}{
   \code{recon()} argument \tab Behaviour \cr
   \code{base} \tab Reference dataset \cr
   \code{compare} \tab Dataset to validate against BASE \cr
   \code{id} \tab Key column(s) for row matching \cr
   \code{var} \tab Restrict comparison to named variables \cr
   \code{base_var} / \code{compare_var} \tab Map differently-named columns \cr
   \code{criterion} \tab Numeric equality tolerance \cr
   \code{relative} \tab Apply tolerance proportionally \cr
   \code{noequal} \tab Suppress equal-variable output \cr
   \code{listall} \tab Print all variable names in header \cr
   \code{brief} \tab Summary section only \cr
   \code{out} \tab Write report to text file \cr
}
}

\author{
Your Name \email{your.email@example.com}
}
\keyword{internal}
