% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recon.R
\name{recon}
\alias{recon}
\title{Compare two data frames}
\usage{
recon(
  base,
  compare,
  id = NULL,
  var = NULL,
  base_var = NULL,
  compare_var = NULL,
  criterion = 1e-08,
  relative = FALSE,
  noequal = FALSE,
  listall = FALSE,
  brief = FALSE,
  out = NULL,
  out_data = TRUE
)
}
\arguments{
\item{base}{A \code{data.frame}  the reference (BASE) dataset.}

\item{compare}{A \code{data.frame}  the dataset to compare against BASE.}

\item{id}{Character vector of one or more key variable name(s) used to
match rows across datasets. If \code{NULL} (default), rows are matched
positionally (row 1 vs row 1, row 2 vs row 2, etc.).}

\item{var}{Character vector of variable names to include in the value
comparison. Defaults to all common variables excluding \code{id} variables.
Equivalent to specifying a variable list in the comparison procedure.}

\item{base_var}{Character vector of variable names in BASE to map to the
corresponding entries in \code{compare_var}. Allows comparison of
differently-named columns that represent the same concept. Must be the
same length as \code{compare_var}.}

\item{compare_var}{Character vector of variable names in COMPARE to map to
the corresponding entries in \code{base_var}. Must be the same length as
\code{base_var}.}

\item{criterion}{Numeric tolerance for comparing numeric variables.
Differences \verb{<= criterion} are treated as equal. Default \code{1e-8}.}

\item{relative}{Logical. If \code{TRUE}, \code{criterion} is applied as a relative
(proportional) difference: \verb{|a - b| / max(|a|, eps)}. Default \code{FALSE}
(absolute difference).}

\item{noequal}{Logical. If \code{TRUE}, suppress printing of variables with
no differences in the output report. Default \code{FALSE}.}

\item{listall}{Logical. If \code{TRUE}, list all variable names in each dataset
in the header section of the report. Default \code{FALSE}.}

\item{brief}{Logical. If \code{TRUE}, print only the summary section and
suppress the row-level difference table. Default \code{FALSE}.}

\item{out}{Optional file path (character string). When supplied, the
complete report is written to that file in addition to being printed.}

\item{out_data}{Logical. If \code{TRUE} (default), the differences data frame
is included in the returned list as \verb{$value_diffs}.}
}
\value{
A named list of class \code{"rDataRecon_result"} (returned
\strong{invisibly}) with elements:

\describe{
\item{\code{summary}}{Named list of dataset-level counts.}
\item{\code{vars_base_only}}{Character vector  variables in BASE only.}
\item{\code{vars_compare_only}}{Character vector  variables in COMPARE only.}
\item{\code{type_mismatches}}{\code{data.frame} with columns \code{variable},
\code{base_class}, \code{compare_class}.}
\item{\code{value_diffs}}{\code{data.frame} with columns \code{obs_id}, \code{variable},
\code{base_value}, \code{compare_value}, \code{diff} (or \code{NULL} if
\code{out_data = FALSE}).}
\item{\code{verdict}}{Either \code{"DATASETS ARE IDENTICAL"} or
\code{"DATASETS HAVE DIFFERENCES"}.}
}
}
\description{
\code{recon()} is the core function of the \pkg{rDataRecon} package.
It compares a \strong{BASE} data frame against a \strong{COMPARE} data frame and
produces a structured four-section report covering:
\itemize{
\item Dataset-level summary (observation counts, variable counts)
\item Variables present in one dataset but not the other
\item Variable class / type mismatches
\item Row-level value differences with absolute or relative numeric deltas
\item A final \strong{IDENTICAL / HAS DIFFERENCES} verdict
}

The function is inspired by traditional dataset-comparison procedures
available in statistical programming environments, and exposes an
equivalent feature set entirely within base R.
}
\section{Argument quick-reference}{
\tabular{ll}{
   Argument \tab Purpose \cr
   \code{base} \tab Reference dataset \cr
   \code{compare} \tab Dataset to validate against BASE \cr
   \code{id} \tab Key column(s) for row matching \cr
   \code{var} \tab Restrict comparison to named variables \cr
   \code{base_var} \tab Rename BASE column(s) before comparison \cr
   \code{compare_var} \tab Rename COMPARE column(s) before comparison \cr
   \code{criterion} \tab Numeric equality tolerance (default 1e-8) \cr
   \code{relative} \tab Apply tolerance proportionally \cr
   \code{noequal} \tab Suppress equal-variable output \cr
   \code{listall} \tab Print all variable names in header \cr
   \code{brief} \tab Summary section only \cr
   \code{out} \tab Write report to a text file \cr
}
}

\examples{
base_df <- data.frame(
  id   = 1:4,
  age  = c(25L, 30L, 35L, 40L),
  name = c("Alice", "Bob", "Carol", "Dave"),
  stringsAsFactors = FALSE
)
comp_df <- data.frame(
  id   = 1:4,
  age  = c(25L, 31L, 35L, 40L),
  name = c("Alice", "bob", "Carol", "Dave"),
  stringsAsFactors = FALSE
)

# Basic comparison with a key variable
result <- recon(base_df, comp_df, id = "id")

# Absolute tolerance of 0.01
recon(base_df, comp_df, id = "id", criterion = 0.01)

# Relative 5 \% tolerance
recon(base_df, comp_df, id = "id", criterion = 0.05, relative = TRUE)

# Cross-variable name mapping
base2 <- data.frame(id = 1:3, revenue = c(100, 200, 300))
comp2 <- data.frame(id = 1:3, sales   = c(100, 250, 300))
recon(base2, comp2, id = "id",
      base_var = "revenue", compare_var = "sales")

}
